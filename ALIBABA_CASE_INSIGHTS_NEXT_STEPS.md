# 从售后备件预测题得到的后续研究与实现方向

## 1. 工作背景

本项目研究零售与售后供应链中的 **forecast-to-decision** 问题：在需求被库存状态和到货时序影响的环境中，如何将可在决策时获得的需求、库存与 ETA 信息转化为可验证的库存风险预警和补货决策。

项目目前有两条互补的证据线：

- **风险预警线**：以 FreshRetailNet-50K 的门店 × 商品 × 小时数据为基础，对仍在售商品预测未来短窗口内的售罄风险与首次售罄时间；
- **补货决策线**：以 Zhao SKU×月面板为主，结合 Mendeley、OSA 与 Lokad 等外部或合成基准，研究需求分位数、在途订单 ETA 不确定性与 order-up-to / `(s,S)` 补货策略之间的接口。

共同的业务难点是：观测到的销量或履约量可能受库存可用性限制，因而并不完整等于真实需求；同时，库存、在途订单、需求结构和新品/新地点都会随时间变化。

## 2. 原始目标

近期阶段目标是在与任务匹配的数据集上复现可比较的预测结果，并建立一条从预测到库存决策的可复现实验路径。该路径应当：

1. 严格限定预测时点的信息集；
2. 在时间外样本上比较需求预测、缺货风险预测和库存决策；
3. 将预测输出转化为明确的服务目标、库存覆盖量与补货动作；
4. 在需求、库存和 ETA 压力下报告服务—成本权衡，而不仅报告平均预测误差；
5. 为未见商品、地点和业务结构变化提供可检验的泛化证据。

### 2.1 数据集能力与分工

当前保留数据集的字段能力不同，应按其可以支持的结论分工，而不以单一数据集承担全部问题。

| 数据集 | 已核对的库存/供给字段 | 可以承担的角色 |
| --- | --- | --- |
| FreshRetailNet-50K / FreshRetailNet-LT | 小时级 `hours_stock_status` 与缺货小时数；**没有库存数量、采购订单、到货日期或 ETA** | 复现截断需求/售罄风险预测与时间外泛化；不用于数量型补货回放 |
| Zhao SKU×月 | `beginning_inventory`、`on-order_inventory`、销售、采购 `order_date`/`arrival_date`、数量、成本与正常提前期；库存快照为月度、无 `store_id` | 主要的真实 SKU×月需求预测与单期补货代理实验；不从月度快照推断日库存路径 |
| Mendeley Retail Transactions and Stocks | 门店-SKU 的 `Qty on hand`、库存区间、销售和成本；没有采购订单、到货日期或 ETA | 外部门店-SKU 库存/销量与可用性验证；不用于 ETA 或真实到货决策结论 |
| OSA-Data | 日级 `on_hand_inventory_units`、`replenishment_units`、`inventory_pipeline`、在途/在订数量、门店-SKU 销量和供应商提前期 | 具有完整状态字段的合成机制与压力测试基准 |
| Lokad distribution-network workshop | `StockOnHand`、采购单日期、`DeliveryDate`、订货/到货数量及销售订单 | 具有完整状态字段的合成分销网络机制与 ETA 压力测试基准 |

没有任何一个当前保留的真实零售数据集提供与售后备件题相同的“独立于履约量的全部确认请求”标签。因此，真实数据上的缺货时段需求仍应作为受截断的观测量处理；合成基准用于验证完整库存—订单—到货机制，不替代真实零售外部验证。

#### 2.1.1 合成与真实必须分开陈述——本项目决策层证据的真实厚度

这是关于本项目**证据基础**的结论，不是实现细节，故置于此并在论文中显式陈述。

| 数据集 | 性质 | 在决策层能承载的结论 |
| --- | --- | --- |
| Zhao | **真实**（JD.com） | 唯一的真实数据预测—决策接口；但标签是**观测销量下界**，且为**单期代理**（层 B） |
| Lokad | **合成**（官方文档称该公司 fictitious） | 仅机制与敏感性证据 |
| OSA | **合成**（提前期表已实测判定装饰性） | 仅机制与敏感性证据，且提前期为**声明情景**而非数据 |
| FreshRetailNet | 真实 | 不进入决策层（无数量型库存/PO/ETA） |
| Mendeley | 未声明 | 不进入决策层（同上） |

由此得出三条必须写进论文的限定：

1. **Lokad 与 OSA 的服务—成本结果只作机制与敏感性证据**，不构成对真实零售环境的外部验证。二者均为合成数据，其成本参数、提前期与需求路径都是设定而非观测。
2. **真实数据上的预测—决策接口结论只有 Zhao 承载**，而它的标签仍是观测销量下界、决策层仍是每月重置的单期代理、测试窗仅两个月。
3. 因此，**本项目在真实数据上的决策层证据比原计划要薄。** 这一点应当在论文里明说，而不是靠三个数据集的数量制造充分性的印象——"五个数据集、三个进入决策层"的表述会让读者误以为决策结论有三重真实验证，实际上只有一重，且是受截断的单期代理。

正确的表述方式是：预测层由五个数据集（含两个真实、一个官方评估切分）支撑；决策层的**真实**证据只有 Zhao 一个数据集的单期代理，合成基准负责验证机制在完整库存—订单—到货状态下的行为。若审稿人要求真实数据上的连续补货因果证据，本项目现有数据**无法提供**，须如实声明而非以合成结果代替。

### 2.2 推荐的起步顺序

1. **FreshRetailNet-50K / LT**：先复现可比较的截断需求或售罄风险预测结果，固定预测时点、历史窗口、缺货掩码、时间外切分和冷启动切片；
2. **Zhao SKU×月**：随后复现结构化需求分位数基线，并将其接入月度覆盖与单期补货代理；这是主要的真实数据预测—决策接口；
3. **OSA 与 Lokad**：在完整库存—补货—到货字段下进行 ETA、截断和服务目标压力测试；
4. **Mendeley**：作为门店-SKU 外部库存/销量可迁移性检查。

### 2.3 Zhao 的建模粒度：采用 SKU × 月，不重建日库存

**可以直接使用月度。** Zhao 原始文件的销售记录与采购订单带有日日期，但 `beginning_inventory`、`on-order_inventory` 与陈列状态只在每月快照中提供（2019-01 至 2019-10）。在没有日初库存、月内库存流水或缺货时点的情况下，把月度库存插值为每日库存会人为假设月内销售、到货和缺货的先后顺序，并可能把事后信息带入预测时点。

因此，Zhao 的一个预测样本定义为 `(sku_ID, month t)`：在月初 $t$ 的信息集下，预测该日历月的累计**观测销售量**，并输出 $q_{.50}$ 与 $q_{.85}$。月初库存/在订快照是状态特征；日销售与采购记录仅按月聚合以形成过去需求、已完成收货和下月前可知的订单信息。Zhao 上的库存结论限定为月度、单期的预测—补货代理；需要日级 ETA、月内缺货和连续库存路径的机制结论，仍由 OSA 与 Lokad 承担。

### 2.4 参照售后备件题补全的首轮数据预处理协议

这一轮只移植题目已经明确的原则：预测时点信息隔离、输入/标签分离、时间外验证、未见 ID 的稳健处理、分位数输出约束与可复现性。它不增加原始数据中不存在的库存事件或真实需求标签。

| 阿里题中的原则 | 在 Zhao 上本轮补全为 | 不做的事情 |
| --- | --- | --- |
| 每行是“实体 × 预测时点”的事前快照 | 使用每个可用月初库存快照形成 `(sku_ID, month t)` 行；`sku_ID` 完整保留为字符串，月度主键必须唯一 | 不把月库存插值为日库存，不制造日级快照 |
| 标签是预测时点后的固定窗口需求 | 标签为 $t$ 月内日销售量按 SKU 求和；它明确命名为 `observed_sales_next_month`，而非“真实需求” | 不把观测销售直接改称潜在真实需求 |
| 所有特征只来自预测时点前 | 月初快照可用；销售只使用 $t-1$ 及更早月份；历史销售构造 1、3、6 月滞后/滚动特征；已完成收货只累计实际到货月早于 $t$ 的订单 | 不把 $t$ 月销售、$t$ 月实际到货或未来订单写入特征 |
| 已下单未到货数量是事前供给状态 | 使用月初 `on-order_inventory` 作为原始在订快照；若使用订单明细，只保留 `order_date < t` 的记录并按月聚合 | 阿里题没有 `arrival_date` 或 ETA 字段；不把 Zhao 的 `arrival_date`、`normal_lead_time` 或推算 ETA 误写为阿里题直接给定的信息 |
| 统一、可审计的输入字段 | 统一原始销售列的拼写变体（`quantity`/`quantiity`、`sales_revenue` 的变体），日期标准化为月初，静态 SKU 属性与月度状态分别连接；交易明细按 SKU×月聚合 | 不用未来全量数据拟合类别映射、缩放器或标签统计量 |
| 训练、验证、测试严格按时间顺序 | 所有编码器、缺失处理器、残差分位数校准器只在训练窗拟合；验证窗选模型与策略，之后测试窗冻结；短时间跨度采用滚动起点报告各可用预测月 | 不随机打散月份；不以测试销售量选择特征、模型或分位数 |
| 正式集可能出现新实体 | 类别/ID 编码在训练窗建立；验证/测试中新 SKU、类别缺失或未见值映射到统一未知值，并单独报告冷启动切片 | 不用全样本 target encoding 或按测试标签补全新 SKU |
| 输出完整、有限、可复现 | 每个预测行保留唯一 `(sku_ID, month t)` 键，输出有限且非负的 $q_{.50},q_{.85}$，并强制 $q_{.50}\le q_{.85}$；固定随机种子与配置 | 不因单个模型失败静默丢行或用测试集修正结果 |

#### 2.4.1 在订、收货与 ETA 的统一特征口径

参照阿里题的 `open_purchase_qty`、历史收货与距上次收货时间，按数据集能力建立下表的**事前状态特征**。不存在相应字段的数据集不以插值或标签反推补齐。

| 数据集 | `open_purchase_qty` 对应项 | 实际收货状态 | 承诺 ETA / 提前期信号 |
| --- | --- | --- | --- |
| Zhao SKU×月 | 月初 `on-order_inventory`，作为主特征 | `arrival_date` 视为实际到货；仅用早于预测月 $t$ 的到货构造过去 1/3/6 月收货量与距上次收货月数 | 对已在 $t$ 前下单且尚未到货的订单，承诺 `ETA = order_date + normal_lead_time`；按承诺到货所在月份聚合在订量、逾期在订量与预计到货量 |
| OSA-Data | `units_on_order`，并与 `units_in_transit`、`inventory_pipeline` 分开保留 | `replenishment_units` 构成已实现日收货 | `LEAD_TIME_ON_ORDER`、`LEAD_TIME_IN_TRANSIT` 与 `LEAD_TIME_IN_DC` 是阶段化提前期信号 |
| Lokad workshop | 对预测日已下单且未关闭的 PO，以未交付量 `OrderQty-DeliveryQty` 构造；与来源 `StockOnHand` 分开 | `DeliveryQty` 与历史 `DeliveryDate` 构成已实现收货 | `DeliveryDate` 的业务语义只在 Workshop 情景中使用；内部调拨提前期按历史订单—接收时间估计 |
| FreshRetailNet / Mendeley | 不支持 | 不支持完整采购收货状态 | 不支持 |

Zhao 的 `arrival_date` 是**事后实际到货结果**，不作为未来预测特征直接输入。离线构造“该订单截至 $t$ 尚未到货”时，只使用这一时点可观察的状态；所有未来到货日期保持不可见。承诺 ETA 是独立特征，严格由 `order_date + normal_lead_time` 推算。数据审计显示两者并不等同：174,155 条 Zhao 订单中，实际到货与该承诺日期相同的比例约为 58.8%，提前约 10.8%，延迟约 30.3%。因此另构造截至 $t$ 已完成订单的历史 ETA 误差（实际到货减承诺 ETA）的滚动中位数与高分位数，作为 ETA 风险特征；不使用任何 $t$ 后才发生的误差。

月初 `on-order_inventory` 是 Zhao 的首选 `open_purchase_qty`，因为它是直接快照。由订单明细重建的未交付量只用于一致性审计和 ETA 分桶，不能静默替代该快照；若二者不一致，保留快照并报告差异。对预测月 $t$，至少输出 `open_purchase_qty`、过去 1/3/6 月实际收货量、`months_since_last_receipt`、承诺在 $t$ 月/下月到货的在订量、逾期在订量与历史 ETA 误差特征。

##### 统一到全部数据集的「origin 时点可观察」纪律

上述 Zhao 规则不是 Zhao 专属，而是一条通用判据，全部具备采购/收货字段的数据集一律适用：

> **可用**：某状态在 origin 时点**能否被当时的观察者读出**。
> **不可用**：任何取值只有在 origin 之后才确定的字段，无论它在文件里是否已经存在。

据此，「该订单截至 origin 尚未到货」是**可用**的（观察者在 origin 当天就知道它还没到），而「它将在哪一天到货」「它最终交付了多少」「它最终是否关单」都是**不可用**的。

| 数据集 | 在订量（主特征） | 判定"截至 origin 未到货"的可观察谓词 | 明令禁止的最终态字段 |
| --- | --- | --- | --- |
| Zhao | 月初 `on-order_inventory` 快照 | `order_date < t` 且 `arrival_date >= t` | 未来 `arrival_date` 本身；由重建量替代快照 |
| OSA | 日表 `units_on_order` 快照 | 快照即当期状态，无需重建 | 把 `units_in_transit`/`units_in_dc`/`inventory_pipeline` 相加为单一在途量 |
| Lokad | 未交付 PO 的 `OrderQty` **全额** | `Date < origin` 且 `DeliveryDate >= origin`（含 `2001-01-01` 哨兵＝从未交付） | **`IsClosed`**（实测为抽取时刻最终状态）；**`OrderQty - DeliveryQty`**（用最终交付量抵扣尚未发生的交付） |

##### 到货日期语义：两个数据集均已实测判定为「实际到货」

「到货日期」这一列名只在 Zhao（`arrival_date`）与 Lokad（`DeliveryDate`）存在，二者语义**均已由数据判定为事后实际到货**，因此适用同一条回溯限制。判据不靠字段名，靠可检验的特征：

| 数据集 | 字段 | 判定 | 证据 |
| --- | --- | --- | --- |
| Zhao | `arrival_date` | 实际到货 | 若为承诺则应恒等于 `order_date + normal_lead_time`；实测仅 58.9% 相等，且右偏（延迟 30.3% vs 提前 10.8%） |
| Lokad | `DeliveryDate` | 实际到货 | ① 918 条哨兵 `2001-01-01`，其中 `IsClosed=False` 占 **100%**——承诺日期不会对未完成订单缺失；② 非哨兵 24,102 条中 `DeliveryQty>0` 占 **99.9%**；③ 同一 `(Supplier,Type)` 内 lag **不恒定**（唯一取值数中位 13、标准差中位 3.7 天、ORDER 型跨 10–70 天），承诺日期应为 `Date+固定提前期` 即 `nunique≈1` |

OSA **没有到货日期字段**，只有 `replenishment_units`（逐日已实现收货数量）；FreshRetailNet 与 Mendeley 二者皆无。

##### 全项目到货 / ETA 可得性总表（含数据性质与已实测的失效项）

| 数据集 | 实际到货 | 承诺 ETA / 提前期 | ETA 误差可估 | 数据性质 |
| --- | --- | --- | --- | --- |
| **Zhao** | ✅ `arrival_date`，逐订单，实测判定为实际 | ✅ `normal_lead_time`，逐订单**数据给定** | ✅ | **真实**（JD.com，论文附录） |
| **Lokad** | ✅ `DeliveryDate`，逐 PO，实测判定为实际 | ⚠️ **无字段**，须由 `(Supplier,Type)` 历史 lag **估计** | ⚠️ 含估计器自身误差 | **合成**（官方文档称该公司为 fictitious） |
| **OSA** | ❌ **无到货日期**，只有 `replenishment_units` 数量 | ❌ **有表但已判定装饰性**，不可用 | ❌ | **合成**，提前期表与流水无关 |
| **FreshRetailNet** | ❌ 无 | ❌ 无 | ❌ | 真实（Meituan） |
| **Mendeley** | ❌ 无（无任何 order/arrival/delivery/lead 列） | ❌ 无 | ❌ | 未声明 |

##### 判定序列可用性时，须区分「交易行数」与「序列长度」

这是本项目已经犯过一次的错误，记录以免重演：Mendeley 每序列中位 **2.5 交易行**，据此被判为「上下文长度 2、TSFM 不可用」。该推理**错误**——数的是交易行，不是序列长度。

判据是**表的性质**：Mendeley 销售表是**交易日志**（125,751 行中仅 2 行 `Qty Sold == 0`），零值不被记录，故「无记录日 = 零销量」可辩护，序列长度应取生命期内的日历天数。对照 OSA：其日表**显式记录零**（48.45% 的行 `total_sales_units == 0`），故「无记录 ≠ 零需求」，缺失必须排除。**同样是「稀疏」，两者的正确处理相反。**

更正后的 Mendeley 画像：序列生命期中位 15 天，但 **23.0%（11,616 条）≥100 天**；这些序列的 ADI 中位 **46.3 天**（Syntetos-Boylan 间歇阈值为 1.32，超出 35 倍）。即**长度可得、信息量近乎为零**——100 天里约 2 个非零事件。

因此 Mendeley 的定位改为：**极端间歇需求专项**（回答"TSFM 是否优于 Croston/TSB"），不是主预测实验；补零只能在生命期内进行；「生命期≥100天」是显式筛选，结论不外推到其余 77%。决策层判定不变（库存语义门 BLOCKED、无 PO/ETA）。

**OSA 提前期表的失效证据**（四条，实测）：① 表中 $L$ 中位 14 天，而 `units_on_order`→`replenishment_units` 的最大互相关滞后中位仅 4 天；② 表值命中经验最优滞后的序列占比 **0.000**（100 条无一命中）；③ $L_{\text{table}}$ 处互相关系数中位 **0.020**；④ `LEAD_TIME_IN_DC ≡ LEAD_TIME_IN_TRANSIT + 1` 在全部 100 行精确成立——这是一次随机抽样加固定偏移的合成签名，不是真实供应链。

据此，OSA 的 `capabilities.eta` 由 `true` 改为 `false`，`purchase_orders` 由 `true` 改为 `partial`（只有数量快照，无订单明细与到货日期）。其保护期 $L$ 降级为**显式声明的情景参数**（网格 7/14/21 天），层 C 回放仍可进行——它本就是固定情景下的机制实验——但结论须与「提前期为研究设定而非数据」一并呈现。

合成与真实的分开陈述要求见 §2.1.1——本节的 ETA 可得性差异正是该要求的一个来源。

##### 承诺 ETA 侧存在不对称，不得并列声称同等可靠

实际到货侧三者一致（只可回溯），但**承诺侧的可得性完全不同**：

| 数据集 | 承诺 ETA 来源 | 性质 | ETA 误差可估？ |
| --- | --- | --- | --- |
| Zhao | `order_date + normal_lead_time`（逐订单给定） | **数据给定** | ✓ 实际减承诺 |
| OSA | 静态提前期表 `LEAD_TIME_*`（每 store×item 一行） | **数据给定** | ✗ 无逐订单实际到货日期，无法构造误差 |
| Lokad | **无任何提前期字段**，须由 origin 前已完成 PO 的历史 lag 估计 | **本项目估计量** | ✓ 但误差中含估计器自身误差 |

Lokad 的估计器固定为：按 `(Supplier, Type)` 取 `DeliveryDate < origin` 的已完成 PO 的 lag 中位数，expanding 窗，组内不足 30 条退到 `Type` 级再退到全局。**必须至少按 `Type` 分组**——实测 `TRANSFER` 的 lag 高度集中（p50=5、p85=7、组内标准差 1.8 天），`ORDER` 离散得多（p50 24–40 天、p85 37–62 天、标准差 5.5–18 天），混合会同时高估调拨、低估采购。截至 replay 起点 2022-10-03，14 个组的最小样本量为 244，全部满足阈值。

因此报告中**禁止**把三者的 ETA 特征并列比较其贡献：Zhao 的承诺是给定事实，Lokad 的是估计量，OSA 只有承诺侧、没有误差侧。

Lokad 的两条禁令由实测确立：`IsClosed=False` 的 `DeliveryDate` 跨 `2001-01-01`(哨兵) 至 `2023-01-08`，说明该标志是抽取时刻状态而非 origin 时点状态；且存在 1,470 条 `IsClosed=False` 但 `DeliveryQty>0` 的部分到货与 986 条短量关单，而每张 PO 只有单一 `DeliveryDate`、没有分次交付事件流，故 origin 时点的"已部分交付量"不可重建——首轮按整单处理，该近似须在报告中声明。

**快照优先于重建**同样通用：凡数据集直接给出在订快照（Zhao 月初 `on-order_inventory`、OSA 日表 `units_on_order`），该快照即主特征；由订单明细重建的未交付总量只用于一致性对账与 ETA 分桶，须报告二者差异，不得静默替代。按承诺 ETA 分桶得到的「本月/下月预计到货量、逾期在订量」是允许的独立特征，因为它们只依赖 `order_date + 提前期`，与实际到货无关。

**该纪律不止于采购字段——「静态属性」不等于「随时可用」。** 判据是取值**何时确定**，不是字段在表里是否随时间变化。任何编码事件日期的静态属性都须按 origin 截断：

- 已实测的例子：Zhao `sup-0004` 的 `stop_year`。562 个 SKU 的 `stop_year = 2019`（另有 140 个 > 2019），落在数据期内；在 2019 年的任一 origin 上「该 SKU 将于 2019 年停产」不可观察。面板中 3,653 行（1.5%）受影响，且该值与目标强相关（已停产行目标均值 2.4，未停产 43.3，相差 18 倍）——是会抬高分数的真实泄漏。
- 正确构造：`known = stop_year < year(origin)`，据此派生 `stop_year_obs`、`is_discontinued`、`years_since_stop`；原始列不得直接入特征。
- 同类需检查的字段：各数据集的 `introduction_year`、生命周期阶段、上市/下市标记、供应商终止日期等。凡语义为「某事件发生于哪一年/哪一天」者，一律按此办理。

**这条容易被漏掉，因为静态属性通常被默认为「先验已知」。** 但先验已知的是*属性存在*，不是*事件何时发生*；后者只有发生后才可观察。

这也修正了 Zhao 的简单历史基线名称：原始时间跨度只有十个月，尚不支持 12 个月季节性朴素法。首轮使用“上一可观测月”与 1/3/6 月滚动历史基线；只有在补入足够长的同频历史后，才将年度季节性朴素法加入 Zhao 比较。**这一替换只适用于 Zhao；其他经数据审计确认拥有至少一个完整同频年度周期的数据集，保留原有年度季节性朴素基线。**

### 2.5 Zhao 的最终月度训练、验证与测试边界

Zhao 只覆盖 2019-01 至 2019-10。为使 6 月历史基线首次在验证窗可用，同时保留两个完全靠后的测试月，固定采用以下**扩展窗口、滚动起点**协议：

| 阶段 | 预测起点与用途 | 在该起点可使用的历史 |
| --- | --- | --- |
| 初始训练/上下文 | 2019-01 至 2019-06 | 形成首个完整的 6 月历史；模型、编码器和残差校准器仅从当时及更早的已观测数据拟合 |
| 验证 | 2019-07、2019-08 | 选择模型类别、特征规则、分位数校准规则和服务约束；7 月预测只能使用 1–6 月，8 月预测只能使用 1–7 月 |
| 冻结测试 | 2019-09、2019-10 | 所有选择在验证结束后冻结；9 月预测只能使用 1–8 月，10 月预测只能使用 1–9 月 |

测试采用**预序（prequential）滚动评估**：10 月起点可使用已经在真实业务中于 9 月末获得的 9 月销售/状态，但不得根据 9 月测试得分重新选择模型、特征、分位点、成本情景或策略。报告 9 月、10 月分别的结果及其合并结果，避免两个测试月被一个平均数掩盖。1 月与 3 月历史基线在历史充足后分别启用；6 月历史基线从 7 月起点起才参与验证和测试。由于测试月只有两个，结论必须同时给出按 SKU 分组的不确定性，而不能宣称稳健的长期时间外泛化。

### 2.6 Lokad 兼容的外部成本敏感性：可做，但不当作 Zhao 的事实

可以参考 Lokad 进行**外部敏感性测试**，但其数值只能标为情景假设。Lokad 的 Workshop #3 设定为无欠单（缺货即丢失销售）和年持有成本为采购价的 20%，并使用单位调拨成本和 ROI；它没有为 Zhao 提供可识别的真实缺货罚金或欠单成本。

因此，Zhao 的主报告仍采用“服务水平—月度持有成本”Pareto 结果，不固定短缺罚金或欠单机制。补充敏感性情景可令 SKU $i$ 的月持有成本为

\[
h_i^{(a)}=a\,c_i/12,\qquad a\in\{0.10,0.20,0.30\},
\]

其中 $c_i$ 是 Zhao 的 `unit_cost`，$a=.20$ 是 Lokad 兼容的中心情景。该测试只改变持有成本，并在预先设定的服务目标下比较策略；不假定月内需求均匀，不把未观测销售当作真实短缺，也不构造欠单。若未来有独立的短缺成本/服务合同资料，才另行定义缺货罚金与欠单场景。

### 2.7 连续多期反事实回放：由本研究构建，不冒充来源复现

FreshRetailNet 原始工作是“小时级缺货掩码下的需求恢复，再做 7 日需求预测”，没有库存数量、采购订单、ETA、成本或补货策略回放。Lokad Workshop #3 虽有库存与订单字段，但其决策部分是固定日期下的一次调拨案例：设定上次调拨在 7 天前、每 4 周调拨一次，以提前期加两个调拨周期内需求的 p95 识别过量库存；它不是跨多月、由策略不断改变库存状态的反事实仿真。

因此，连续回放设为本研究新增的**机制实验层**，并与来源复现和 Zhao 主实验分开：

| 实验层 | 数据集与时间粒度 | 允许的结论 |
| --- | --- | --- |
| A. 预测/删失层 | FreshRetailNet 小时/日，Zhao 月，Mendeley 日或库存区间 | 分位数、WAPE/pinball、校准和可观测性；不主张策略因果收益 |
| B. 单期决策代理层 | Zhao 月初快照 | 在固定月初库存、`open_purchase_qty` 与承诺 ETA 情景下，比较 order-up-to 目标、服务约束与持有成本敏感性；每月起点重置为观测快照，不连接为连续库存路径 |
| C. 连续机制回放层 | OSA 日级、Lokad 合成网络日级 | 固定起点库存和外生需求路径，策略在每个预设 review 时点下单，订单按预设 ETA/提前期进入库存；库存状态由策略自身递推，不再重置为历史库存快照 |

层 C 的最小设计固定如下：每个起点使用至少 84 天（3 个四周调拨周期）的路径，而不是 Lokad 的单个 28 天案例；所有候选策略共享初始库存、需求路径、提前期/延迟情景、订单成本和随机种子。每期先接收已到货订单，再满足固定需求路径；缺货按预先声明的 lost-sales 情景处理，最后按策略生成新订单。OSA 使用其日级在订/在途/补货与分阶段提前期字段；Lokad 使用 `StockHistory`、PO、调拨提前期与单位调拨成本。两者都报告服务率、填补率、平均库存、持有/调拨成本、订单事件数和服务—成本 Pareto 前沿。

真实需求仍不可由已交付销售自动识别。故层 C 的基础报告称为“观测销售路径回放”；若加入高需求或截断压力，只能作为明确标注的合成敏感性情景，不能称作真实缺货成本或真实服务率。Zhao 不进入层 C，避免月度快照被伪造成连续日库存。

### 2.8 数据审计的结论与仍然待决的事项

**已解决（2026-08-22 实测）：`arrival_date` 是事后实际到货，不是事前承诺日期。** 判据：若它是承诺日期，则应恒等于 `order_date + normal_lead_time`；实测 174,155 条订单中仅 58.8% 相等，且偏差右偏（延迟 30.3%、提前 10.8%）。承诺日期不会产生这样的分布。补充实测：`normal_lead_time` 中位 1 天（p75=2、max=13），实际到货天数中位 2 天，ETA 误差 $\Delta_{.50}=0$、$\Delta_{.85}=3$、$\Delta_{.95}=5$ 天。

因此 §2.4.1 建立在该判定上的规则（`arrival_date` 不作为 $t$ 后订单特征、承诺 ETA 独立由 `order_date + normal_lead_time` 推算、ETA 误差滚动分位数作为风险特征）**成立**，二者不再矛盾。

仍然待决：

1. Zhao 订单的取消、拆单、部分到货规则——`arrival_date` 的语义已定，但一张订单是否可能对应多次到货尚未审计；若存在，`quantity` 与实际收货量的对应关系需重新定义；
2. Zhao 月内真实库存路径、缺货日期和潜在未满足需求；
3. 若要在 Zhao 上使用短缺罚金或欠单，所需的独立业务成本来源与机制定义。**注意**：`docs/07_decision_layer.md` §5.3 采用的是由服务目标 $\alpha$ 经 newsvendor 临界比导出的 $p_i=h_i\alpha/(1-\alpha)$，它是**导出量而非业务事实**，不构成对本条的解决。

## 3. 从售后备件真实需求预测题提炼出的核心命题

售后备件题将预测单位定义为“仓库 × 备件 × 预测时点”的历史快照，并要求预测未来 7 天 **全部确认请求量** 的 p50 与 p85。它提供了一个可直接迁移到本项目的建模框架：

> 预测对象应是条件真实需求的分位数；库存可用性、历史履约和运营计划既是需求可观测性的条件，也是未来需求和决策压力的信号。

这一定义使三个对象保持分离：

| 对象 | 在本项目中的含义 | 后续用途 |
| --- | --- | --- |
| 条件需求 | 给定事前信息下、未来覆盖期内的需求分布或分位数 | 需求预测与分位数校准 |
| 供给/可用性状态 | on-hand、库存覆盖、缺货掩码、在途订单与 ETA | 识别观测状态，形成可用库存信息集 |
| 决策目标 | 服务目标、短缺风险、库存成本和行动预算 | 选择需求—ETA—库存覆盖映射 |

## 4. 后续建模方向

### 4.1 显式建模需求可观测性

对每个预测时点，建立统一的 `availability / censoring` 状态表示：

- 当前和历史库存可用性；
- 历史缺货或售罄掩码；
- 历史观测销量/履约量；
- 当前现货、可确定到货的在途库存与 ETA 风险；
- 需求规模、活动、计划维护或其他可在事前确定的需求信号。

需求模型将这些状态作为条件变量，并把销量/履约量解释为受可用性影响的观测量。风险模型继续以“当前仍可售”的样本为预测对象；补货回放继续将观测销量短缺明确视为可识别下界。

### 4.2 以分位数为统一预测接口

在需求预测复现中，固定输出至少包括 p50 与服务目标对应的高分位数（如 p85、p90 或 p95），并在同一时间切分下比较：

- 历史/季节性基线；
- 使用结构化业务状态的 HGB 或 LightGBM 分位数基线；
- TSFM 的需求分位数；
- TSFM 分位数与结构化状态相结合的风险或决策模型。

分位数的统计验证使用 pinball loss、经验覆盖率和分组校准；分位数的运营验证使用服务率、短缺量、库存税与订单行动预算。预测模型与补货策略应分别比较，再评价它们组成的完整决策链路。

#### 4.2.1 按数据集固定的算法与基线

“复现来源工作”的算法与“统一研究比较”的算法分别固定如下。每个数据集只运行与其任务和字段相容的行；所有模型共享同一过去训练—验证—测试切分与同一预测窗口。

| 数据集 | 复现来源工作的必选比较 | 统一研究比较的必选预测基线 | 该数据集上的核心对照 |
| --- | --- | --- | --- |
| FreshRetailNet-50K / LT | **代表性复现集**：原始销量（无恢复）、TimesNet 恢复、iTransformer 恢复、DLinear 恢复；预测端固定比较 SSA、TFT、DLinear | 不替代来源指标；可额外使用 `state-HGB` 与 `state + TSFM pressure-HGB` 做售罄风险增量实验 | 原始销量 vs 恢复需求；TimesNet（论文中恢复 WAPE 最优）vs iTransformer（预测偏差接近零）vs DLinear（可解释低复杂度）；无 TSFM 状态头 vs 加入 TSFM 分位数/压力特征的状态头 |
| Lokad workshop | Workshop #3 的既有 `WeeklyForecast` 网络库存/调拨分析；Workshop #4 的 $\exp(\mathrm{level})\times\mathrm{seasonality}\times\mathrm{linear\ trend}$ 点预测及其概率化版本 | 不再扩展额外统计模型；只比较既有 `WeeklyForecast`、Workshop #4 低维参数概率模型和本研究的分位数模型 | 三种预测输入在 CRPS、可用率、缺货/积压、持有成本和 ROI 上比较 |
| Zhao SKU×月 | 无官方预测基线 | 上一可观测月/滚动历史基线；结构化 LightGBM 或 HGB 分位数回归；零样本或轻量适配的 TSFM 分位数 | 历史与类别/月初库存/在订订单特征的分位数 GBDT vs 仅依赖历史序列的 TSFM；随后将各自分位数送入同一月度单期补货代理 |
| Mendeley 门店-SKU | 无官方预测基线 | 季节性朴素预测；Croston-SBA 与 TSB 间歇需求基线；结构化 LightGBM 或 HGB 分位数回归；TSFM 分位数 | 重点比较间歇需求统计基线、全局结构化模型和 TSFM 在稀疏序列、未见 SKU/门店上的稳定性；只做库存/销量外部验证 |
| OSA-Data | 无官方预测基线 | 移动平均/季节性朴素预测；Croston-SBA 与 TSB（对稀疏 SKU）；结构化 LightGBM 或 HGB 分位数回归；TSFM 分位数 | 需求层比较统计、GBDT 与 TSFM；风险层固定比较 `state-HGB` 与 `state + TSFM pressure-HGB`，隔离 TSFM 分位数带来的增量 |

统一研究比较中的 LightGBM/HGB 必须直接训练 $q_{.50}$ 与 $q_{.85}$。季节性朴素、滚动均值、Croston-SBA 与 TSB 是点预测基线；进入双分位数 pinball 比较时，只能使用前置训练/验证窗口的残差分布将其校准为 $q_{.50}$ 与 $q_{.85}$，不得使用测试标签。TSFM 若输出样本或完整预测分布，取相应经验分位数；若只输出点预测，也采用同一前置残差校准规则。

这种按数据集选择基线不是遗漏 FreshRetailNet 或 Lokad 的算法，而是保证比较与数据机制相容：FreshRetailNet 的需求恢复模型服务于其特定的缺货截断与多模态观测任务；Lokad Workshop #3 的 `WeeklyForecast` 是给定输入，Workshop #4 的低维模型也依赖其教程中的业务设定，二者都不是可在 Zhao 或 Mendeley 上公平重训的通用预测器。故它们分别在来源复现中保留；Zhao 采用“上一可观测月/滚动历史—分位数 GBDT—TSFM”的最小比较，Mendeley 再加入 Croston-SBA 与 TSB 以覆盖间歇需求。原文模型结果用于说明代表性模型的选择，不构成把所有来源模型搬到全部数据集的理由。

TFT、深度多任务模型或更复杂的端到端策略模型作为第二阶段扩展，只有在上述统计、结构化 GBDT 和 TSFM 基线均已完成并通过相同压力测试后才进入比较。

#### 4.2.2 最小而有代表性的复现原则

FreshRetailNet 原论文列出的 TimesNet、SAITS、ImputeFormer、iTransformer、GPVAE、CSDI 与 DLinear 是完整的来源比较表。本研究不逐一重跑全部模型，而是采用上述代表性复现集：

1. **原始销量**：衡量不处理截断时的端到端基准；
2. **TimesNet**：原论文需求恢复中 WAPE 最优的代表；
3. **iTransformer**：原论文后续预测中偏差接近零的强代表；
4. **DLinear**：低复杂度、可解释的线性代表；
5. **SSA、TFT、DLinear 预测器**：分别代表场景统计、深度协变量时序模型和线性预测模型。

实际运行采用一个稀疏的端到端设计，而不是“所有恢复模型 × 所有预测模型”的全组合：`Raw + TFT`、`TimesNet + TFT`、`iTransformer + TFT`、`TimesNet + DLinear`、`TimesNet + SSA`，再加本研究的 TSFM/结构化风险模型。论文中把原始完整比较表作为文献基准，并将本项目表述为 **代表性复现与扩展**，而非完整复现。

Lokad Workshop #3 本身使用已给定的 `WeeklyForecast`，并不提供第二个待训练的预测算法。因此网络库存实验只保留三种预测输入：既有 `WeeklyForecast`、Workshop #4 的低维参数概率模型，以及本研究的分位数模型。这样仍能检验“不同预测接口是否改变库存决策”，而不会把教程数据扩展为无必要的大型模型竞赛。

### 4.3 将业务机制变化视为一等测试场景

时间外测试集不只报告总体指标，还应预先定义并报告下列压力切片：

1. **低可用性/严重截断**：库存覆盖低、缺货掩码多或库存压力高的样本；
2. **需求构成变化**：促销、活动、维护计划、季节或其他事前需求信号占比上升的样本；
3. **新品与新地点**：未见 SKU、未见门店/仓库、未见组合，以及长尾序列；
4. **ETA 与提前期压力**：正常、轻度延迟和重度延迟下的物理到货回放；
5. **关键业务单元**：高价值、高缺货代价或高服务目标 SKU 的服务和成本表现。

每个压力切片都应同时报告预测分位数表现和决策表现，以区分“预测误差变化”与“服务/库存后果变化”。

### 4.4 使用可外推的业务强度特征

除原始销量、库存和类别变量外，结构化基线与联合模型应优先包含具有业务解释、可在新 SKU/地点上计算的比例和压力特征，例如：

- 需求事件强度 / 设备、合同或门店覆盖规模；
- 缺货天数或售罄小时占可观测窗口的比例；
- 历史履约/销量与可用性共同表达的需求压力代理；
- 已计划活动、维护或促销相对服务规模的强度；
- 可用现货加上符合覆盖期条件的在途库存；
- ETA 延迟分位数转化后的额外需求覆盖期。

这些特征与门店、SKU 等标识信息并行使用。标识信息可提升已见单元的拟合；类别、区域、规模和压力特征承担新品、新地点与时间外场景中的迁移能力。

## 5. 后续实验设计

### 5.1 两类评价协议

本项目同时追求“可复现既有工作”与“建立统一的 forecast-to-decision 比较”。两者的目标、标签可识别性和指标不同，必须分开报告。

#### 协议 A：复现原始数据源工作

复现阶段优先使用来源论文或官方工作流的原始任务定义和评价指标：

| 数据源 | 复现对象 | 原始或官方评价 |
| --- | --- | --- |
| FreshRetailNet-50K / LT | 缺货截断下的需求恢复与后续需求预测 | 需求恢复使用 WAPE、WPE 与需求—缺货解耦分数 $\rho_{DS}$；预测在无缺货期使用 WAPE、WPE |
| Lokad workshop | 概率需求预测与基于预测的库存网络分析 | 点预测模型以 MSE 拟合；概率需求分布使用 CRPS；库存分析同时关注可用率、缺货/积压、持有成本与 ROI |
| Zhao SKU×月 | 数据准备、时间切分与公开字段的可重建性 | 原始数据论文未提供预测模型排行榜或统一预测损失 |
| Mendeley | 数据准备、门店-SKU 面板与库存/销售口径的可重建性 | 数据发布未提供官方预测基线或评价准则 |
| OSA-Data | OOS/OSA 状态、库存与提前期字段的可重建性 | 官方材料是模拟数据分析工作流，未提供预测模型排行榜或统一预测损失 |

因此，协议 A 的结论只能是“是否复现了该来源的任务和指标”；不能把不同来源的 WAPE、WPE、CRPS、可用率或成本数值直接并列为同一个模型排名。

#### 协议 B：统一研究比较

在协议 A 的数据口径和时间切分得到复现后，使用统一的双分位数 pinball 预测层与服务—成本决策层，回答需求分位数、库存状态和 ETA 信息如何共同影响补货决策。协议 B 不替代任何来源的原始评价，而是本研究新增的跨模型比较协议。

双分位数是**最小且固定的比较接口**，而不是声称两个分位数已完整刻画需求分布：$q_{.50}$ 检验中心位置，$q_{.85}$ 提供统一的上尾压力检验。两者适合所有能构造相同预测目标、并有真实需求或仿真真值的数据切片。预测目标必须与决策时点对齐：要么直接预测 review period 加有效提前期内的累计需求，要么由逐日/逐周预测样本在同一覆盖期内卷积得到该累计需求分布；不能用单日 $q_{.85}$ 直接驱动多日补货。

### 5.2 数据与切分

- 采用严格的过去训练、随后验证、再后测试的时间顺序；
- 在验证窗选择模型、分位数校准规则与服务目标下的库存策略；
- 测试窗只用于冻结评估：模型类别、特征、分位数和策略选择均不得据测试得分调整；若采用预序滚动评估，只能按预先写定的规则用上一已结束测试期的真实历史重拟合下一起点，不能改变选择本身；
- 保持训练、预测、压力情景和随机种子的完整可复现记录。

### 5.3 统一研究比较的两层评估

**预测层**：以双分位数的加权归一化 pinball loss 为主指标，并报告覆盖率、校准曲线和冷启动表现。对真实目标 $y_i$、预测分位数 $q_{i,\tau}$ 和分位点 $\tau$，定义：

\[
\ell_{\tau}(y_i,q_{i,\tau})
=\max\{\tau(y_i-q_{i,\tau}),\;(\tau-1)(y_i-q_{i,\tau})\}.
\]

每个场景同时评估中位数 $q_{.50}$ 与固定的上尾分位数 $q_{.85}$。这使跨数据集和跨模型的首轮比较可复现；它不是把 $85\%$ 指定为所有 SKU 的服务目标。若某策略的预先设定周期服务目标为 $\alpha$（例如 $90\%$ 或 $95\%$），决策输入应使用 $q_{\alpha}$ 或完整预测分布，并将相应 pinball loss 作为预先声明的补充报告。场景 $\mathcal{S}$ 的损失定义为两项 pinball loss 的加权和，再按该场景加权的、带逐样本地板的观测需求归一化：

\[
\operatorname{NPL}_{.50,\gamma}(\mathcal{S})
=
\frac{
\sum_{i\in\mathcal{S}} w_i\left[\ell_{.50}(y_i,q_{i,.50})+\ell_{\gamma}(y_i,q_{i,\gamma})\right]
}{
\max\!\left(2\sum_{i\in\mathcal{S}} w_i\max(y_i,\,y_{\min}),\;\varepsilon\right)
}.
\]

其中 $w_i$ 是关键业务单元的业务权重，$\gamma=0.85$，$\varepsilon=10^{-12}$ 只用于分母的数值下限，$y_{\min}$ 是逐数据集固定的最小业务单位。**地板 $\max(y_i,y_{\min})$ 在求和内部逐样本生效**，与售后备件题 judge 的形式一致；不采用「按场景标量均值 $\bar y_w$ 归一化」的写法，因为后者对单行零需求没有保护，只在整个场景全零时才触发 $\varepsilon$，而本项目要跑 Croston-SBA/TSB 的间歇需求数据集（§4.2.1），并要求按低可用性、冷启动等切片分别报告（§5.4），近乎全零的切片会使该分母趋于零。

$y_{\min}$ 不可跨数据集照搬。它按确定性规则在**训练窗**上选定：于阶梯 $\{1.0,0.5,0.1,0.05,0.01\}$ 中取地板触发率 $\Pr(y_i<y_{\min})\le 10\%$ 的最大值，选定后冻结，禁止在见到任何测试窗结果后修改。

| 数据集 | 目标量纲 | $y_{\min}$ |
| --- | --- | --- |
| Zhao SKU×月 | 月销量，整数件 | 1.0 |
| OSA-Data | 日销量，整数件 | 1.0 |
| Mendeley | 日净销量，整数件 | 1.0 |
| Lokad | 周销量，整数件 | 1.0 |
| FreshRetailNet-50K / LT | `sale_amount`，连续量 | 由上述规则在原始审计阶段选定；实测该列约 91% 为非整数、样本中位数约 0.7，取 $y_{\min}=1$ 会使过半训练行被地板值支配 |

**NPL 不可分解。** 每个切片使用该切片自身的分母，因此整体 NPL 不等于各切片 NPL 的加权平均；禁止由切片值平均出总值，也禁止反向拆解。又因 $w_i$ 方案与 $y_{\min}$ 逐数据集不同，NPL 只在同一数据集、同一场景内比较模型，不构成跨数据集排名。

权重方案在实验前固定并冻结：切点只在训练窗计算一次，原样应用到验证与测试窗；FreshRetailNet 与 OSA 无成本字段，取 $w_i\equiv 1$，其 NPL 退化为无权重形式，报告中须注明而不与加权数据集并列排名。$w_i$ 的逐数据集实例化、$y_{\min}$ 的选取规则与本式的完整约束见 `docs/00_global_conventions.md` §3–§4。

该指标的适用边界也需固定：在存在独立真实需求标签或可识别仿真真值的场景中，它衡量条件需求分位数；在 FreshRetailNet、Zhao 和 Mendeley 的缺货/售罄时段，观测销量仍是受截断下界。因此此时报告的 pinball loss 必须标注为“观测销量下界上的预测损失”，并与缺货风险、服务和短缺下界等指标共同解释，不能替代对潜在真实需求的识别。

**决策层**：这是一个预先固定的、按时间推进的补货策略回放，不是将预测误差事后换算成成本。在每个补货时点，策略只能使用该时点前已知的库存、在途订单、ETA 和预测分位数；先按固定的 order-up-to 或 $(s,S)$ 映射给出订货量，再沿同一需求路径与物理到货轨迹推进库存。对每个模型都使用同一初始库存、同一订单约束、同一 ETA 情景、同一需求路径和同一策略候选集。

服务指标至少区分周期服务率（周期内未缺货的比例）与数量服务率/填补率（满足需求量占总需求量的比例）；二者不能混用。成本指标由期末或平均在手库存的持有成本、采购/调拨成本、短缺或欠单成本，以及必要时的订单固定成本组成；数据未提供的成本参数不伪造为事实，而是在验证窗前固定为透明的敏感性网格。报告服务—成本 Pareto 前沿、订单事件率和服务目标可行性，而不是只报告一个任意加权总分。需求分位数通过覆盖期映射、ETA 安全库存和 lead-time-demand convolution 等候选规则进入策略；这些规则在验证窗选择后冻结。

在 OSA 与 Lokad 的仿真/完整状态机制中，回放可使用可识别的需求和到货轨迹解释真实服务与短缺。在 FreshRetailNet、Zhao 和 Mendeley 的缺货时段，真实未满足需求不可观测，决策层只能报告观测短缺下界、库存可用性风险和对成本参数的条件性反事实结果，不能宣称得到真实填补率或真实缺货成本。

### 5.4 固定的场景验收

每次模型比较都应通过下列验收，而不是仅比较一个总体平均分数：

**验收按数据集能力裁剪，不是所有场景对所有数据集都适用。** 结构性不支持的场景直接跳过并记 `BLOCKED`（退出码 2），不记 `FAIL`——`configs/<ds>.yaml` 的 `capabilities` 是唯一判据。

| 场景 | 预测层报告 | 决策层报告 | 适用数据集（预测层） | 适用数据集（决策层） |
| --- | --- | --- | --- | --- |
| 常规时间外周期 | 加权归一化双分位数 pinball loss、覆盖率 | 服务率、库存税、短缺下界 | 全部 5 个 | zhao(层B)、osa、lokad |
| 低可用性/高截断 | 同一指标及与常规场景的差异 | 服务率与短缺下界的变化 | fresh（显式缺货掩码）；zhao、osa、lokad（由 on-hand 派生的代理）；mendeley 受库存语义门约束 | zhao、osa、lokad |
| 需求构成漂移 | 高活动/促销或其他事前需求事件强度子集的损失 | 服务目标可行性与成本变化 | fresh（`activity_flag`/`discount`）、osa（`units_under_promotion`）、mendeley（`Stock Status` 的 Promo/Markdown/Clearance）；zhao 仅陈列位代理；**lokad 无此信号** | osa |
| 新 SKU/门店/组合 | 已见与未见单元分别报告损失 | 冷启动单元的服务—成本表现 | 全部 5 个 | zhao、osa、lokad |
| 关键业务单元 | 按预先固定权重的损失与覆盖率 | 高价值/高缺货代价单元的服务水平 | zhao、mendeley、lokad（有成本字段）；**fresh、osa 的 $w_i\equiv1$，该场景退化为整体，不单列** | zhao、lokad |
| ETA/提前期压力 | 需求分位数的稳定性 | 固定物理到货轨迹下的服务—成本 Pareto 前沿 | **zhao**（承诺 ETA 数据给定）、**lokad**（承诺 ETA 为本项目估计量）；**osa 提前期表已判定装饰性、fresh 与 mendeley 无字段——三者均不适用** | lokad（层 C，估计 ETA）；zhao（层 B，承诺 ETA 情景）；**osa 只能跑声明情景网格 7/14/21 天，不得计为 ETA 压力验收** |

上表把原先"每次模型比较都须通过全部六个场景"改为按能力裁剪。原表在 5 个数据集中有 3 个结构性不可能通过，属验收设计缺陷。

此外，每次运行校验输入输出样本一一对应、分位数非负、$q_{.50}\le q_{\gamma}$、随机种子固定、训练与预测可在预设资源约束内稳定完成。

### 5.5 服务约束下的选择

继续使用前置验证窗，在候选策略集合中选择满足服务目标且成本最低的策略；若服务目标不可行，则显式保留不可行状态。候选集合应包含：

- 直接需求分位数 order-up-to；
- 分位数与 ETA 延迟覆盖期映射；
- 安全库存近似；
- lead-time-demand convolution；
- `(s,S)` 与 order-up-to 两种动作机制。

这样，研究主张聚焦于“在给定信息集和服务目标下，哪类接口提供更稳健的服务—库存权衡”，而不是宣称某个分位数或库存公式在所有环境中最优。

## 6. 建议的执行顺序

1. 在 FreshRetailNet-50K / LT 冻结数据字典、时间切分、预测目标和可用信息集，复现截断需求或售罄风险基线；
2. 在 Zhao SKU×月复现历史基线、结构化分位数 GBDT 基线与 TSFM 分位数输出；
3. 建立库存可用性/截断、需求构成、冷启动和 ETA 压力的统一分组诊断；
4. 将 Zhao 上表现稳定的分位数输出接入统一决策层，并用 OSA/Lokad 复核完整供给机制下的行为；
5. 在验证窗完成服务约束选择，在冻结测试窗输出预测层与决策层的成对结果；
6. 以压力切片、服务可行性和 Pareto 前沿组织论文与管理含义，并以 Mendeley 报告可迁移性边界。

## 7. 工程协作与复现协议

后续实现按“定义—实现—测试—诊断—单点迭代”推进：

1. 先复述数据口径、标签、信息集和验收指标；
2. 每次只实现一个模块，例如数据切分、特征接口、分位数基线或决策映射；
3. 每个模块完成后独立运行对应的格式、时间切分和分组测试；
4. 由诊断结果选择唯一的下一项改动；
5. 保存模型配置、随机种子、特征清单、选择规则和每次测试的结果快照。

这套协议使各数据集上的可比较预测结果、压力测试和决策回放共享同一套可审计的证据链。
